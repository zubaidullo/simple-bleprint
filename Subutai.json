{
  "name": "${environmentName}",
  "description": "Environment configuration test blueprint",
  "nodes": [
    {
      "name": "backup-server",
      "template": "ubuntu16",
      "peer_group": "HTTP-GROUP",
      "size": "${type}"
    },
     {
      "name": "backup-server1",
      "template": "ubuntu16",
      "peer_group": "HTTP-GROUP",
      "size": "${type1}"
    },
     {
      "name": "backup-server2",
      "template": "ubuntu16",
      "peer_group": "HTTP-GROUP",
      "size": "${type2}"
    },
     {
      "name": "backup-server3",
      "template": "ubuntu16",
      "peer_group": "HTTP-GROUP",
      "size": "${type3}"
    }
    ,
     {
      "name": "backup-server54",
      "template": "ubuntu16",
      "peer_group": "HTTP-GROUP",
      "size": "${type4}"
    }
     ,
     {
      "name": "backup-server545",
      "template": "ubuntu16",
      "peer_group": "HTTP-GROUP",
      "size": "${type5}"
    }
    
  ],
  "peerGroups": [
    {
      "name": "HTTP-GROUP",
      "pricingPref": {
         "maxPrice": "${maxPrice}"
      }
    }
  ]
}
