{
  "name": "${environmentName}",
  "description": "Environment configuration test blueprint",
  "nodes": [
    {
      "name": "${containerName}",
      "template": "ubuntu16",
      "peer_group": "HTTP-GROUP",
      "size": "${backupContainerType}"
    }
  ],
  "peerGroups": [
    {
      "name": "HTTP-GROUP",
      "pricingPref": {
         "maxPrice": "${maxPrice}"
      }
    }
  ],
  "backupContainerType": "you can set TINY,SMALL,MEDIUM,LARGE,HUGE",
  "containerName" : "Enter container name here",
  "maxPrice": "Peers max price"
}
