{
  "name": "${environmentName}",
  "description": "Environment configuration test blueprint",
  "nodes": [
    {
      "name": "backup-server",
      "template": "ubuntu16",
      "peer_group": "HTTP-GROUP",
      "size": "${ubuntu16ContainerType}"
    }
  ],
  "peerGroups": [
    {
      "name": "HTTP-GROUP",
      "pricingPref": {
         "maxPrice": "${maxPrice}"
      }
    }
  ]
}
